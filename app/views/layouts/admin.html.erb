<!DOCTYPE html>
<html lang="en" class="h-full" data-controller="theme">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard | <%= Rails.application.class.module_parent_name %></title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%# Theme initialization happens via theme/init.js - imported at the top of application.js %>
  <script>
    // Add class to prevent flash during initial load
    document.documentElement.classList.add('theme-initializing');

    // Remove initializing class when DOM is loaded
    document.addEventListener('DOMContentLoaded', function() {
      setTimeout(function() {
        document.documentElement.classList.remove('theme-initializing');
      }, 50); // Small delay to ensure transitions work
    });
  </script>

  <style>
    /* Block rendering until the theme is applied to prevent flash */
    .theme-initializing {
      visibility: hidden;
    }

    /* Add smooth transitions for theme changes */
    html {
      transition: background-color 0.3s ease, color 0.3s ease;
    }

    /* Ensure basic theme classes are applied immediately */
    html.dark {
      background-color: #111827;
      color: #ffffff;
    }

    html {
      background-color: #ffffff;
      color: #111827;
    }
  </style>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700;800&family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">

  <%# Important: theme.css must be loaded first to establish base CSS variables %>
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "theme", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "dark_mode_overrides", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "light_mode_fixes", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "section_fixes", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "admin_custom", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "admin_patterns", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "admin_design_system", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

  <!-- Direct fix for theme and dropdowns - no dependencies -->
  <%= render 'layouts/direct_fix' %>

  <!-- Admin-specific styles moved to external CSS files -->
  <style>
    /* Emergency fix for admin layout */
    .admin-sidebar {
      position: fixed !important;
      top: 0 !important;
      left: 0 !important;
      bottom: 0 !important;
      width: 280px !important;
      z-index: 100 !important;
      background-color: white !important;
    }

    .dark .admin-sidebar {
      background-color: #1f2937 !important;
    }

    .admin-content {
      margin-left: 280px !important;
      width: calc(100% - 280px) !important;
      padding-left: 20px !important; /* Add padding to move content away from sidebar */
    }

    .admin-sidebar.collapsed {
      width: 80px !important;
    }

    .admin-content.expanded {
      margin-left: 80px !important;
      width: calc(100% - 80px) !important;
      padding-left: 20px !important; /* Add padding to move content away from sidebar */
    }

    /* Fix for cards to ensure they appear above the sidebar */
    .admin-grid {
      position: relative !important;
      z-index: 200 !important; /* Higher than sidebar */
      margin-left: 10px !important; /* Add margin to move grid away from sidebar */
    }

    .stat-card {
      position: relative !important;
      z-index: 300 !important; /* Even higher than grid */
    }

    /* Ensure all content in the admin area has proper z-index */
    .admin-content > * {
      position: relative !important;
      z-index: 150 !important; /* Higher than sidebar */
    }

    @media (max-width: 1023px) {
      .admin-sidebar {
        transform: translateX(-100%) !important;
      }

      .admin-sidebar.open {
        transform: translateX(0) !important;
      }

      .admin-content {
        margin-left: 0 !important;
        width: 100% !important;
      }
    }
  </style>
</head>

<body class="font-poppins antialiased bg-gray-50 dark:bg-gray-900 text-gray-900 dark:text-white min-h-screen flex flex-col admin-layout" data-theme-target="body" data-controller="icon">
  <%= render 'shared/icons' %>
  <div class="flex h-full">
    <!-- Admin Sidebar -->
    <aside class="admin-sidebar bg-white dark:bg-gray-800 shadow-lg" data-controller="admin-sidebar">
      <!-- Logo and Toggle -->
      <div class="flex items-center justify-between p-4 border-b border-gray-200 dark:border-gray-700 bg-indigo-50 dark:bg-indigo-900/20">
        <div class="flex items-center">
          <div class="flex-shrink-0">
            <%= image_tag "logo.png", class: "h-8 w-auto", alt: "Logo" %>
          </div>
          <div class="ml-3 text-xl font-bold text-indigo-700 dark:text-indigo-300 sidebar-text">Admin</div>
        </div>
        <button type="button" class="text-indigo-600 hover:text-indigo-800 dark:text-indigo-400 dark:hover:text-indigo-300" data-action="admin-sidebar#toggle">
          <%= icon "menu-alt-1", class: "w-6 h-6" %>
        </button>
      </div>

      <!-- Navigation -->
      <nav class="mt-5 px-2 space-y-1">
        <%= link_to admin_dashboard_path, class: "flex items-center px-4 py-3 text-base font-medium rounded-lg #{current_page?(admin_dashboard_path) ? 'bg-indigo-50 text-indigo-700 dark:bg-indigo-900/50 dark:text-indigo-300' : 'text-gray-700 hover:bg-gray-50 dark:text-gray-300 dark:hover:bg-gray-700'}" do %>
          <div class="flex items-center justify-center w-8 h-8">
            <%= icon "chart-bar", class: "w-6 h-6" %>
          </div>
          <span class="ml-3 sidebar-text">Dashboard</span>
        <% end %>

        <%= link_to admin_users_path, class: "flex items-center px-4 py-3 text-base font-medium rounded-lg #{current_page?(admin_users_path) ? 'bg-indigo-50 text-indigo-700 dark:bg-indigo-900/50 dark:text-indigo-300' : 'text-gray-700 hover:bg-gray-50 dark:text-gray-300 dark:hover:bg-gray-700'}" do %>
          <div class="flex items-center justify-center w-8 h-8">
            <%= icon "users", class: "w-6 h-6" %>
          </div>
          <span class="ml-3 sidebar-text">Users</span>
        <% end %>

        <%= link_to admin_transactions_path, class: "flex items-center px-4 py-3 text-base font-medium rounded-lg #{current_page?(admin_transactions_path) ? 'bg-indigo-50 text-indigo-700 dark:bg-indigo-900/50 dark:text-indigo-300' : 'text-gray-700 hover:bg-gray-50 dark:text-gray-300 dark:hover:bg-gray-700'}" do %>
          <div class="flex items-center justify-center w-8 h-8">
            <%= icon "currency-dollar", class: "w-6 h-6" %>
          </div>
          <span class="ml-3 sidebar-text">Transactions</span>
        <% end %>

        <%= link_to admin_scheduled_transactions_path, class: "flex items-center px-4 py-3 text-base font-medium rounded-lg #{current_page?(admin_scheduled_transactions_path) ? 'bg-indigo-50 text-indigo-700 dark:bg-indigo-900/50 dark:text-indigo-300' : 'text-gray-700 hover:bg-gray-50 dark:text-gray-300 dark:hover:bg-gray-700'}" do %>
          <div class="flex items-center justify-center w-8 h-8">
            <%= icon "clock", class: "w-6 h-6" %>
          </div>
          <span class="ml-3 sidebar-text">Scheduled</span>
        <% end %>

        <%= link_to admin_events_path, class: "flex items-center px-4 py-3 text-base font-medium rounded-lg #{controller_name == 'events' ? 'bg-indigo-50 text-indigo-700 dark:bg-indigo-900/50 dark:text-indigo-300' : 'text-gray-700 hover:bg-gray-50 dark:text-gray-300 dark:hover:bg-gray-700'}" do %>
          <div class="flex items-center justify-center w-8 h-8">
            <%= icon "calendar", class: "w-6 h-6" %>
          </div>
          <span class="ml-3 sidebar-text">Events</span>
        <% end %>

        <!-- Shop Management Section -->
        <div class="mt-8 mb-2 px-4">
          <h3 class="text-xs font-semibold text-gray-500 dark:text-gray-400 uppercase tracking-wider sidebar-text">
            Shop Management
          </h3>
        </div>

        <%= link_to admin_products_path, class: "flex items-center px-4 py-3 text-base font-medium rounded-lg #{controller_name == 'products' ? 'bg-indigo-50 text-indigo-700 dark:bg-indigo-900/50 dark:text-indigo-300' : 'text-gray-700 hover:bg-gray-50 dark:text-gray-300 dark:hover:bg-gray-700'}" do %>
          <div class="flex items-center justify-center w-8 h-8">
            <%= icon "shopping-bag", class: "w-6 h-6" %>
          </div>
          <span class="ml-3 sidebar-text">Products</span>
        <% end %>

        <%= link_to admin_orders_path, class: "flex items-center px-4 py-3 text-base font-medium rounded-lg #{controller_name == 'orders' ? 'bg-indigo-50 text-indigo-700 dark:bg-indigo-900/50 dark:text-indigo-300' : 'text-gray-700 hover:bg-gray-50 dark:text-gray-300 dark:hover:bg-gray-700'}" do %>
          <div class="flex items-center justify-center w-8 h-8">
            <%= icon "shopping-cart", class: "w-6 h-6" %>
          </div>
          <span class="ml-3 sidebar-text">Orders</span>
        <% end %>

        <!-- Promotional Features Section -->
        <div class="mt-8 mb-2 px-4">
          <h3 class="text-xs font-semibold text-gray-500 dark:text-gray-400 uppercase tracking-wider sidebar-text">
            Promotions
          </h3>
        </div>

        <%= link_to admin_discounts_path, class: "flex items-center px-4 py-3 text-base font-medium rounded-lg #{controller_name == 'discounts' ? 'bg-indigo-50 text-indigo-700 dark:bg-indigo-900/50 dark:text-indigo-300' : 'text-gray-700 hover:bg-gray-50 dark:text-gray-300 dark:hover:bg-gray-700'}" do %>
          <div class="flex items-center justify-center w-8 h-8">
            <%= icon "percent", class: "w-6 h-6" %>
          </div>
          <span class="ml-3 sidebar-text">Discounts</span>
        <% end %>

        <%= link_to admin_coupons_path, class: "flex items-center px-4 py-3 text-base font-medium rounded-lg #{controller_name == 'coupons' ? 'bg-indigo-50 text-indigo-700 dark:bg-indigo-900/50 dark:text-indigo-300' : 'text-gray-700 hover:bg-gray-50 dark:text-gray-300 dark:hover:bg-gray-700'}" do %>
          <div class="flex items-center justify-center w-8 h-8">
            <%= icon "ticket", class: "w-6 h-6" %>
          </div>
          <span class="ml-3 sidebar-text">Coupons</span>
        <% end %>

        <%= link_to admin_flash_sales_path, class: "flex items-center px-4 py-3 text-base font-medium rounded-lg #{controller_name == 'flash_sales' ? 'bg-indigo-50 text-indigo-700 dark:bg-indigo-900/50 dark:text-indigo-300' : 'text-gray-700 hover:bg-gray-50 dark:text-gray-300 dark:hover:bg-gray-700'}" do %>
          <div class="flex items-center justify-center w-8 h-8">
            <%= icon "bolt", class: "w-6 h-6" %>
          </div>
          <span class="ml-3 sidebar-text">Flash Sales</span>
        <% end %>

        <%= link_to admin_promotional_banners_path, class: "flex items-center px-4 py-3 text-base font-medium rounded-lg #{controller_name == 'promotional_banners' ? 'bg-indigo-50 text-indigo-700 dark:bg-indigo-900/50 dark:text-indigo-300' : 'text-gray-700 hover:bg-gray-50 dark:text-gray-300 dark:hover:bg-gray-700'}" do %>
          <div class="flex items-center justify-center w-8 h-8">
            <%= icon "photograph", class: "w-6 h-6" %>
          </div>
          <span class="ml-3 sidebar-text">Banners</span>
        <% end %>

        <% if ActiveRecord::Base.connection.table_exists?(:loans) %>
          <%= link_to admin_loans_path, class: "flex items-center px-4 py-3 text-base font-medium rounded-lg #{current_page?(admin_loans_path) ? 'bg-indigo-50 text-indigo-700 dark:bg-indigo-900/50 dark:text-indigo-300' : 'text-gray-700 hover:bg-gray-50 dark:text-gray-300 dark:hover:bg-gray-700'}" do %>
            <div class="flex items-center justify-center w-8 h-8">
              <%= icon "credit-card", class: "w-6 h-6" %>
            </div>
            <span class="ml-3 sidebar-text">Loans</span>
          <% end %>
        <% end %>



        <%= link_to admin_system_path, class: "flex items-center px-4 py-3 text-base font-medium rounded-lg #{current_page?(admin_system_path) ? 'bg-indigo-50 text-indigo-700 dark:bg-indigo-900/50 dark:text-indigo-300' : 'text-gray-700 hover:bg-gray-50 dark:text-gray-300 dark:hover:bg-gray-700'}" do %>
          <div class="flex items-center justify-center w-8 h-8">
            <%= icon "server", class: "w-6 h-6" %>
          </div>
          <span class="ml-3 sidebar-text">System</span>
        <% end %>

        <!-- Notifications Link -->
        <%= link_to admin_notifications_path, class: "flex items-center px-4 py-3 text-base font-medium rounded-lg #{controller_name == 'notifications' ? 'bg-indigo-50 text-indigo-700 dark:bg-indigo-900/50 dark:text-indigo-300' : 'text-gray-700 hover:bg-gray-50 dark:text-gray-300 dark:hover:bg-gray-700'}" do %>
          <div class="flex items-center justify-center w-8 h-8 relative">
            <%= icon "bell", class: "w-6 h-6" %>
            <% if @recent_users.present? %>
              <span class="absolute -top-1 -right-1 flex items-center justify-center h-4 w-4 rounded-full bg-red-500 text-white text-xs font-bold shadow-sm"><%= @recent_users.count > 9 ? '9+' : @recent_users.count %></span>
            <% end %>
          </div>
          <span class="ml-3 sidebar-text">Notifications</span>
          <% if @recent_users.present? %>
            <span class="ml-auto bg-red-100 text-red-800 dark:bg-red-900 dark:text-red-300 py-0.5 px-2 rounded-full text-xs font-medium">
              <%= @recent_users.count %>
            </span>
          <% end %>
        <% end %>

        <% if defined?(ContactSubmission) %>
          <%= link_to admin_contact_submissions_path, class: "flex items-center px-4 py-3 text-base font-medium rounded-lg #{controller_name == 'contact_submissions' ? 'bg-indigo-50 text-indigo-700 dark:bg-indigo-900/50 dark:text-indigo-300' : 'text-gray-700 hover:bg-gray-50 dark:text-gray-300 dark:hover:bg-gray-700'}" do %>
            <div class="flex items-center justify-center w-8 h-8">
              <%= icon "mail", class: "w-6 h-6" %>
            </div>
            <span class="ml-3 sidebar-text">Contact Messages</span>
            <% if @unread_contact_submissions && @unread_contact_submissions > 0 %>
              <span class="ml-auto bg-red-100 text-red-800 dark:bg-red-900 dark:text-red-300 py-0.5 px-2 rounded-full text-xs font-medium">
                <%= @unread_contact_submissions %>
              </span>
            <% end %>
          <% end %>
        <% end %>
      </nav>

      <!-- Bottom section -->
      <div class="absolute bottom-0 left-0 right-0 p-4 border-t border-gray-200 dark:border-gray-700 bg-gray-50 dark:bg-gray-700/50">
        <div class="flex items-center">
          <div class="flex-shrink-0">
            <% if current_user.respond_to?(:avatar) && current_user.avatar.attached? %>
              <%= image_tag current_user.avatar, class: "h-10 w-10 rounded-full border-2 border-indigo-200 dark:border-indigo-800", alt: "User avatar" %>
            <% else %>
              <div class="h-10 w-10 rounded-full bg-indigo-100 dark:bg-indigo-900 flex items-center justify-center text-indigo-600 dark:text-indigo-300 font-semibold text-lg border-2 border-indigo-200 dark:border-indigo-800">
                <%= current_user&.username&.first&.upcase || 'U' %>
              </div>
            <% end %>
          </div>
          <div class="ml-3 sidebar-text">
            <p class="text-sm font-medium text-gray-900 dark:text-white"><%= current_user&.username || 'User' %></p>
            <p class="text-xs text-indigo-600 dark:text-indigo-400 font-medium">Administrator</p>
          </div>
        </div>

        <div class="mt-3 space-y-1">
          <%= link_to root_path, class: "flex items-center px-4 py-2 text-sm font-medium rounded-lg text-gray-700 hover:bg-gray-100 dark:text-gray-300 dark:hover:bg-gray-600 transition-colors" do %>
            <div class="flex items-center justify-center w-6 h-6">
              <%= icon "home", class: "w-5 h-5" %>
            </div>
            <span class="ml-3 sidebar-text">Back to Site</span>
          <% end %>

          <%= button_to destroy_user_session_path, method: :delete, class: "w-full flex items-center px-4 py-2 text-sm font-medium rounded-lg text-red-600 hover:bg-red-50 dark:text-red-400 dark:hover:bg-red-900/20 transition-colors" do %>
            <div class="flex items-center justify-center w-6 h-6">
              <%= icon "logout", class: "w-5 h-5" %>
            </div>
            <span class="ml-3 sidebar-text">Sign Out</span>
          <% end %>
        </div>
      </div>
    </aside>

    <!-- Main Content -->
    <main class="admin-content">
      <!-- Top Bar -->
      <div class="sticky top-0 z-10 bg-white dark:bg-gray-800 shadow-md border-b border-gray-200 dark:border-gray-700">
        <div class="flex items-center justify-between h-16 px-4 sm:px-6 lg:px-8">
          <div class="flex items-center">
            <button type="button" class="text-gray-500 hover:text-gray-600 dark:text-gray-400 dark:hover:text-gray-300 lg:hidden" data-action="admin-sidebar#toggle">
              <%= icon "menu", class: "w-6 h-6" %>
            </button>
            <h1 class="ml-4 text-xl font-bold text-gray-900 dark:text-white"><%= yield(:title) || "Admin Dashboard" %></h1>
          </div>

          <!-- Right side of header - can add elements here in the future -->
          <div class="flex items-center">
          </div>
        </div>
      </div>

      <!-- Page Content -->
      <div class="py-6 px-4 sm:px-6 lg:px-12 max-w-full">
        <%= render 'shared/flash' %>
        <%= yield %>
      </div>
    </main>
  </div>

  <%= javascript_tag do %>
    document.addEventListener('turbo:load', function() {
      // Initialize any JavaScript that needs to run on page load
    });
  <% end %>
</body>
</html>
