<!DOCTYPE html>
<html>
<head>
  <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      background-color: #f8f9fa;
      padding: 20px;
      text-align: center;
      border-bottom: 1px solid #e9ecef;
    }
    .content {
      padding: 20px;
    }
    .footer {
      background-color: #f8f9fa;
      padding: 20px;
      text-align: center;
      font-size: 12px;
      color: #6c757d;
      border-top: 1px solid #e9ecef;
    }
    .button {
      display: inline-block;
      padding: 10px 20px;
      background-color: #007bff;
      color: #ffffff !important;
      text-decoration: none;
      border-radius: 4px;
      margin-top: 20px;
    }
    .critical {
      background-color: #dc3545;
      color: #ffffff;
      padding: 10px;
      border-radius: 4px;
      margin-bottom: 20px;
    }
    .warning {
      background-color: #ffc107;
      color: #212529;
      padding: 10px;
      border-radius: 4px;
      margin-bottom: 20px;
    }
    .info {
      background-color: #17a2b8;
      color: #ffffff;
      padding: 10px;
      border-radius: 4px;
      margin-bottom: 20px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1><%= @notification.title %></h1>
    </div>
    
    <div class="content">
      <% if @notification.severity == 'critical' %>
        <div class="critical">
          <strong>URGENT:</strong> This notification requires your immediate attention.
        </div>
      <% elsif @notification.severity == 'warning' %>
        <div class="warning">
          <strong>IMPORTANT:</strong> Please review this notification as soon as possible.
        </div>
      <% end %>
      
      <p><%= @notification.message %></p>
      
      <% if @notification.metadata.present? %>
        <div style="background-color: #f8f9fa; padding: 10px; border-radius: 4px; margin-top: 20px;">
          <% @notification.metadata.each do |key, value| %>
            <p><strong><%= key.to_s.humanize %>:</strong> <%= value %></p>
          <% end %>
        </div>
      <% end %>
      
      <% if @notification.action_url.present? %>
        <p>
          <a href="<%= @notification.action_url %>" class="button">
            <%= @notification.action_text.presence || 'View Details' %>
          </a>
        </p>
      <% end %>
    </div>
    
    <div class="footer">
      <p>This email was sent to <%= @user.email %> because you have notifications enabled for your account.</p>
      <p>To update your notification preferences, <a href="<%= user_settings_notifications_url %>">click here</a>.</p>
      <p>&copy; <%= Date.current.year %> Zongo. All rights reserved.</p>
    </div>
  </div>
</body>
</html>
